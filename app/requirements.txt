# Web Framework
uvicorn==0.30.1
fastapi==0.111.0

# AI/ML Libraries
# Pinned exactly: an unpinned `>=` here let Dokploy and local resolve to different
# versions on separate builds, and older releases used a different internal
# module layout for the Google/Gemini model class — which broke OpenRouter-style
# "provider/model" routing (e.g. "google/gemma-4-31b-it" landing in the native
# Gemini client instead of OpenRouter, 404ing against Google's API).
pydantic-ai-slim[google,anthropic,openai,openrouter,logfire]==2.1.0
pydantic>=2.7.4

# Database
psycopg[binary]==3.3.2
sqlalchemy==2.0.30
asyncpg>=0.29.0

# Environment & Config
python-dotenv==1.0.1

# Data Processing
pandas==2.2.2
faker==25.3.0

# Caching
redis==5.0.6

# HTTP Client
httpx>=0.27.0

# Image/Media Processing
Pillow>=10.0.0

# PDF Processing (for invoices)
reportlab>=4.0.0

# Paystack Integration
requests>=2.31.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Utilities
tiktoken>=0.5.0

# File Processing
PyPDF2>=3.0.0
pdfplumber>=0.10.0
Pillow>=10.0.0
pytesseract>=0.3.10
aiofiles==25.1.0

# Logging & Observability
logfire[fastapi]==4.29.0
